{
  "name": "civic-guardian",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:demo": "npm run build --prefix web-demo && rm -rf frontend/public/civic-guardian/demo && cp -r web-demo/dist frontend/public/civic-guardian/demo && rm -rf cloudflare-dist/* && cp -r frontend/public/civic-guardian/* cloudflare-dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.3",
    "playwright": "^1.62.0"
  }
}
